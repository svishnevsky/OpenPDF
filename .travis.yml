dist: trusty
branches:
  only:
    - master
    - release
    - develop
language: csharp
mono: none
dotnet: 2.1
addons:
  sonarcloud:
    organization: svishnevsky-github
solution: OpenPDF.sln
install:
  dotnet restore OpenPDF.sln
script:
  - sonar-scanner
  - dotnet build OpenPDF.sln
  - dotnet test tests/OpenPDF.Tests.csproj --no-build --logger:trx
cache:
  directories:
    - '$HOME/.sonar/cache'